import 'package:flutter/material.dart';

const mytextFormPadding = EdgeInsets.only(
  left: 15,
  right: 15,
  top: 10,
  bottom: 10,
);
const mycontentPadding = EdgeInsets.only(
  left: 15,
  right: 15,
  top: 10,
);
const mywidgetPadding = EdgeInsets.only(
  left: 15,
  right: 15,
);

const mybuttonPadding = EdgeInsets.only(
  left: 15,
  right: 10,
  bottom: 15,
);
